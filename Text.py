import pygame

#initilizing font
pygame.font.init()


textsurface = myfont.render('Some Text', False, (0, 0, 0))

# Mapping the font to a variable
textsurface = myfont.render('Some Text', False, (0, 0, 0))